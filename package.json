{
  "name": "weather-system",
  "version": "1.0.0",
  "description": "Weather system pipeline (Sensor → Sampler → Transformer → API → DB)",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "test": "jest"
  },
  "dependencies": {
    "pg": "^8.11.5"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  }
}